
web:
    image: nginx:latest
    ports:
     - "8080:80"
    volumes:
     - ./src:/src
     - ./env/etc/nginx/conf.d/:/etc/nginx/conf.d/
    links:
        - php
        
php:
    image: php:7-fpm
    volumes:
     - ./src:/src
     
composer:
    restart: 'yes'
    image: "composer/composer"
    command: create-project symfony/framework-standard-edition src
    volumes:
     - ./:/app